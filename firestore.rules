rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads and writes by default for now.
      // WARNING: This is insecure and should be replaced with proper rules.
      allow read, write: if true;
    }
  }
}
