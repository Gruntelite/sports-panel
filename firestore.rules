rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite la escritura a cualquier usuario autenticado.
    // Permite la lectura a cualquier usuario autenticado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
